/*
interface MyProps {
    isToggleOn: boolean
}
interface MyState {

}

class App extends React.Component<MyState, MyProps>{
    constructor(props: any){
        super(props);
        this.state = {
            isToggleOn: true
        };
    };

    handleClick = () => {
        this.setState({
            isToggleOn: !this.state.isToggleOn
        })
    };

    render(): any {
        return (
            <div>
                <h1>Wesoła Twórczość</h1>
                <button onClick={() => this.handleClick()}>
                    {this.state.isToggleOn? 'Włączony' : 'Wyłączony'}
                </button>
            </div>
        );
    }
}
*/


export {}